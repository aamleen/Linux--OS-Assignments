If you want to change the unsigned long being passed to B, then open A and change the value of num variable.
Then run as described below:

To run the program:
Write make Q2 on the terminal, and then a Q2 executable file will be generated 
Run the executable file by writing ./Q2 on the terminal